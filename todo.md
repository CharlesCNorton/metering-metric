# Research TODO

1. Fix the theory-native closure ratio `Xi_J = eta_J chi_tau / chi_L^2` and the regime of validity of `(tau_p, L_perp) = (chi_tau t_P, chi_L ell_s)` across laboratory, cosmology, and lensing.
2. Close the 3+1D covariant completion as one explicit field system with source transport, Einstein backreaction, and conservation from the same action.
3. Push the Einstein-backreaction branch from residual-class agreement to a quantitatively competitive field-level match.
4. Extend the calibrated cluster model beyond the current best combined branch: `+5 deg` member-frame rotation, `beta_model` smooth envelope, and shifted elliptic LOS `beta_model`; simple principal-axis LOS splitting, simple member-frame rotation alone, and smooth-`beta_model` substitution alone are already tested and do not beat that combined branch.
5. Turn the current domain-scaled species benchmarks into data-anchored source/activity windows for concrete messengers.
6. Extend the Coq development beyond the current admissible-path, mass-hierarchy, energy-hierarchy, and regularized-floor scope.
7. Reopen the observational proxy lane only with genuinely better two-cluster data.
8. Revisit the deferred `N^-4` branch only after the mainline completion is exhausted.
